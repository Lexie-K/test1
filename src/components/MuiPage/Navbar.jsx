import { AppBar, Toolbar } from "@mui/material";
import React from "react";

const Navbar = () => {
  return (
    <AppBar sx={{ bgcolor: "red", color: "black" }}>
      <Toolbar>Navbar</Toolbar>
    </AppBar>
  );
};

export default Navbar;
 